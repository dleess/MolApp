## Codebase Patterns
- The iPadOS app builds through `MolApp.xcodeproj` with shared scheme `MolApp`; use the README `xcodebuild` command with `CODE_SIGNING_ALLOWED=NO` for simulator verification.
- Keep `MoleculeViewerView` viewport-first: the primary viewer should be the back layer in a full-screen `ZStack`, with controls added as overlays instead of sidebars that resize the viewport.
- Bundled WKWebView assets should be added to the Xcode target resources and loaded with `loadFileURL(_:allowingReadAccessTo:)`; Xcode copies `MolApp/Resources/viewer.html` into the app bundle root as `viewer.html`.
- Mol* assets are copied as a `MolApp/Resources/molstar` folder resource, so `viewer.html` should load them with bundle-root-relative paths like `molstar/molstar.js` and `molstar/molstar.css`.
- `MolAppTests` is an XCTest target wired into the shared `MolApp` scheme; use `xcodebuild ... test CODE_SIGNING_ALLOWED=NO` for test verification when app code has test coverage.

## 2026-04-30 17:04:18 KST - US-001
- Implemented a minimal native iPadOS SwiftUI scaffold named MolApp.
- Added `MolAppApp` as the app entry point and routed launch into `MoleculeViewerView`.
- Added a shared Xcode scheme and documented the simulator build command in `README.md`.
- Files changed: `MolApp/MolAppApp.swift`, `MolApp/MoleculeViewerView.swift`, `MolApp.xcodeproj/project.pbxproj`, `MolApp.xcodeproj/xcshareddata/xcschemes/MolApp.xcscheme`, `README.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks run and results: `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' build CODE_SIGNING_ALLOWED=NO` passed.
- Browser verification result: not applicable; US-001 is project scaffolding with no frontend browser surface.
- **Learnings for future iterations:**
  - The available iPad Pro 13-inch simulator runtime is reported by xcodebuild as `26.4.1`, even though the SDK path is `iPhoneSimulator26.4.sdk`.
  - This repo did not previously contain an app project, so future UI stories should extend the new `MoleculeViewerView` rather than adding a separate root.
---
## 2026-04-30 17:06:55 KST - US-002
- Implemented a full-screen SwiftUI molecule viewport placeholder in `MoleculeViewerView`.
- Added compact floating status controls over the viewport without introducing a fixed sidebar or constraining the viewport.
- Files changed: `MolApp/MoleculeViewerView.swift`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks run and results: `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' build CODE_SIGNING_ALLOWED=NO` passed.
- Browser verification result: native iPadOS UI verified in iOS Simulator. Installed and launched `com.example.MolApp` on iPad Pro 13-inch (M5), OS 26.4.1; portrait screenshot showed the viewport filling the screen behind floating overlay controls. Browser verification is not applicable because this story changes native SwiftUI UI, not a web surface.
- **Learnings for future iterations:**
  - Future viewer controls should be overlaid on `MoleculeViewerView` rather than placed in sidebars, preserving the full-screen viewport contract.
  - The available simulator tooling in this environment can install, launch, and screenshot the native app for UI checks.
---
## 2026-04-30 17:10:02 KST - US-003
- Implemented a SwiftUI-compatible `WKWebView` wrapper for the molecule viewport.
- Added bundled local `viewer.html` with a full-viewport Mol* host placeholder and wired it into the app target resources.
- Replaced the SwiftUI-only viewport placeholder with the local HTML-backed web view while preserving overlay controls.
- Files changed: `MolApp/MolStarWebView.swift`, `MolApp/MoleculeViewerView.swift`, `MolApp/Resources/viewer.html`, `MolApp.xcodeproj/project.pbxproj`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks run and results: `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' build CODE_SIGNING_ALLOWED=NO` passed.
- Browser verification result: native WKWebView verification completed in iOS Simulator. Installed and launched `com.example.MolApp` on iPad Pro 13-inch (M5), OS 26.4.1; screenshot showed the local HTML Mol* viewport placeholder rendering full-screen behind the native overlay.
- **Learnings for future iterations:**
  - `WKWebViewConfiguration.defaultWebpagePreferences.allowsContentJavaScript = true` is available with the current iPadOS deployment target and is the narrow setting needed before Mol* JavaScript is bundled.
  - Resource lookup uses `Bundle.main.url(forResource: "viewer", withExtension: "html")`, so future Mol* assets should either live beside the copied HTML resource or update the read-access directory deliberately.
---
## 2026-04-30 17:17:12 KST - US-004
- Bundled Mol* viewer assets locally under `MolApp/Resources/molstar`.
- Updated `viewer.html` to load the local Mol* CSS and JavaScript, initialize `molstar.Viewer`, fill the WKWebView viewport, and show a persistent visible error state if initialization fails.
- Added the Mol* asset folder to the Xcode target resources and documented the folder resource path convention.
- Files changed: `AGENTS.md`, `MolApp/Resources/viewer.html`, `MolApp/Resources/molstar/molstar.css`, `MolApp/Resources/molstar/molstar.js`, `MolApp.xcodeproj/project.pbxproj`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks run and results: `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' build CODE_SIGNING_ALLOWED=NO` passed.
- Browser verification result: temporary Playwright with system Google Chrome opened `MolApp/Resources/viewer.html`; `document.documentElement.dataset.viewerState` reached `ready`, `window.molapp.viewer` existed, the host measured 1024x768, and one canvas rendered. Native simulator smoke check installed and launched `com.example.MolApp` on iPad Pro 13-inch, OS 26.4.1; screenshot showed the full-screen Mol* viewport without the initialization error overlay.
- **Learnings for future iterations:**
  - Keep Mol* debug/status UI outside `#molstar-host`; `Viewer.create` replaces host contents during startup.
  - `viewportBackgroundColor` should be passed as a CSS color string such as `#0b0f14`; passing a numeric color triggered `e.toLowerCase is not a function` in Mol* 5.8.0.
  - Playwright browser installation was blocked by a local certificate chain issue, but using the installed `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` executable with a temporary Playwright package worked for local file verification.
---
## 2026-04-30 17:22:46 KST - US-005
- Implemented a Swift-to-JavaScript bridge with JSON-encoded command envelopes for `loadLocalStructure`, `loadPdbId`, `setRepresentation`, `toggleVisibility`, `focusSelection`, `setSelection`, and `clearSelection`.
- Added a Mol* viewer-side command dispatcher that reports structured success and failure messages back through the WKWebView script message handler, including non-crashing unknown-command failures.
- Added an XCTest target and focused bridge decoding tests so the shared scheme now has a real test action.
- Files changed: `MolApp/MolStarBridge.swift`, `MolApp/MolStarWebView.swift`, `MolApp/MoleculeViewerView.swift`, `MolApp/Resources/viewer.html`, `MolAppTests/MolStarBridgeTests.swift`, `MolApp.xcodeproj/project.pbxproj`, `MolApp.xcodeproj/xcshareddata/xcschemes/MolApp.xcscheme`, `AGENTS.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks run and results: `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' build CODE_SIGNING_ALLOWED=NO` passed; `xcodebuild -project MolApp.xcodeproj -scheme MolApp -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5),OS=26.4.1' test CODE_SIGNING_ALLOWED=NO` passed.
- Browser verification result: temporary Playwright with system Google Chrome opened `MolApp/Resources/viewer.html`; `document.documentElement.dataset.viewerState` reached `ready`, an unknown command returned a structured failure, and `toggleVisibility` returned success without console or page errors.
- **Learnings for future iterations:**
  - Send native viewer commands as one JSON envelope to `window.molapp.handleNativeCommand(...)`; do not interpolate command payload fields into JavaScript snippets.
  - JavaScript command responses should post `{ id, command, success, error }` through `window.webkit.messageHandlers.molapp`.
  - Mol* command handlers can start as bridge-safe no-ops for future UI stories, but they should still return structured success or failure so native controls can surface errors consistently.
---
